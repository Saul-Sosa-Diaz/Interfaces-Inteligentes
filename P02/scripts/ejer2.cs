using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ejer2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("I am " + name + "!");   
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("I am " + name + " again!");
    }
}
